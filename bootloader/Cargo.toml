[package]
name = "xnuboot"
version = "0.1.0"
edition = "2021"

[dependencies]
uefi = { version = "0.36", features = ["global_allocator"] }
uefi-raw = "0.13"
goblin = { workspace = true, features = ["mach64"] }
log = "0.4"

[lints]
workspace = true
